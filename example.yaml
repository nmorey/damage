config:
        libname: sigmac
entries:
        - name: DB
          attribute: TOP
          fields:
                - name: field1
                  type: T(unsigned long)
                  default: 0UL
                  quantity: SINGLE
                - name: field2
                  type: S(timestamp)
                  default: 0UL
                  quantity: SINGLE
        
        
