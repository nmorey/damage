config:
        libname: test
entries:
        - name: top_struct
          attribute: top
          fields:
                - name: field1
                  type: T(unsigned long)
                  default: 0UL
                  quantity: SINGLE
        
        
